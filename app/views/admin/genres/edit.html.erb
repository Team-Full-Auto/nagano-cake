<div class='container px-5'>
  <div class='row'>
    <h2>ジャンル編集</h2>
      <%= form_with model: @genre do |f| %>
        <%= f.label :ジャンル名 %>
        <%= f.text_field :name %>
        <%= f.submit "変更を保存" class: "btn btn-success" %>
      <% end %>
  </div>
</div>